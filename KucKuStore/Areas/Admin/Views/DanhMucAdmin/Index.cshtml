@model PagedList<KucKuStore.Models.Entities.DANHMUC>
@using PagedList.Mvc
@using PagedList
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}


<div class="animated fadeIn">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <strong>SẢN PHẨM</strong>
                    <h3>
                        @Html.ActionLink("Thêm mới", "Create", "DanhMucAdmin")
                    </h3>
                </div>
                <div class="card-body">
                    <table id="bootstrap-data-table" class="table table-striped table-bordered">
                        <tr>
                            <th>
                                Tên danh mục
                            </th>
                            <th>
                                Tiêu đề ngắn
                            </th>
                            <th>
                                Trạng thái
                            </th>
                            <th>
                                Chức năng
                            </th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TENDM)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TIEUDENGAN)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TRANGTHAI)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.MADM }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.MADM }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.MADM })
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div><!-- .animated -->


    @Html.PagedListPager(Model, pageTemp => Url.Action("Index", new { pageTemp = pageTemp }), new PagedListRenderOptions()
{
    LiElementClasses = new List<string> { "tav_button", "tav_item" }
})
  